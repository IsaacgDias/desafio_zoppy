<div class="flex flex-col items-center justify-center min-h-screen bg-gray-100 p-4">
  <div class="w-full max-w-5xl bg-white rounded-lg shadow-md p-6">
    <!-- Título -->
    <h2 class="text-2xl font-semibold text-gray-800 mb-4 text-center">Produtos</h2>

    <!-- Filtro e Botões -->
    <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center mb-4 gap-2">
      <input
        type="text"
        placeholder="Filtrar produtos..."
        [(ngModel)]="filtroProdutos"
        (ngModelChange)="filtrarProdutos()"
        class="w-full sm:w-1/3 px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-400 focus:border-teal-400"
      />

      <div class="flex gap-2 mt-2 sm:mt-0">
        <button
          (click)="novoProduto()"
          class="bg-gradient-to-r from-teal-400 via-teal-500 to-blue-400 text-white font-semibold px-4 py-2 rounded-full hover:opacity-90 transition"
        >
          Novo Produto
        </button>
        <button
          (click)="irParaClientes()"
          class="bg-gray-300 text-gray-800 font-semibold px-4 py-2 rounded-full hover:bg-gray-400 transition"
        >
          Lista de Clientes
        </button>
      </div>
    </div>